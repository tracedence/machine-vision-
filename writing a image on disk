import cv2
capture = cv2.VideoCapture(0)
ret, frame = capture.read()
# write image in disk with myimg name and jpg type
cv2.imwrite('myimg'+ '.jpg', frame)
        
cap.release()
cv2.destroyAllWindows()
